using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WanderPattern : MonoBehaviour
{
    //Vector3 OriginPos;

    //float MoveSpeed = 1f;
    //private void Awake()
    //{
    //    OriginPos = transform.position;
    //}

    ////private void Update()
    ////{
    ////    Wander();
    ////    //InvokeRepeating("Wander", 0, 5);
    ////}

    //public void RandomWander()
    //{
    //    Vector3 position = new Vector3(Random.Range(-5, 5), 0, Random.Range(-5, 5));

    //    transform.position = Vector3.Lerp(transform.position, position, MoveSpeed * Time.deltaTime);
    //}

    //public void Wander()
    //{
        

    //    //if (transform.position.x == -4.9f)
    //    //    transform.position = Vector3.Lerp(transform.position, new Vector3(-5, 0, 5), MoveSpeed * Time.deltaTime);

    //    //if (transform.position.z == 4.9f)
    //    //    transform.position = Vector3.Lerp(transform.position, OriginPos, MoveSpeed * Time.deltaTime);
    //}
}

